# Обязательные переменные
# ======================

# Registration token от GitHub (получить в Settings -> Actions -> Runners -> New runner)
# ВАЖНО: Токен действителен только 1 час!
TOKEN=your_registration_token_here

# URL репозитория или организации
# Примеры:
#   - Для репозитория: https://github.com/owner/repo
#   - Для организации: https://github.com/org-name
GITHUB_URL=https://github.com/your-org-or-user/your-repo


# Опциональные переменные
# ========================

# Имя runner'а (по умолчанию: hostname контейнера)
RUNNER_NAME=my-docker-runner

# Рабочая директория для runner'а
RUNNER_WORKDIR=_work

# Группа runner'ов (для организаций)
RUNNER_GROUP=Default

# Метки для runner'а (через запятую)
RUNNER_LABELS=docker,self-hosted,linux,ubuntu-22.04

# Ephemeral режим: runner обрабатывает только один job и удаляется
# Рекомендуется для Docker контейнеров
EPHEMERAL=true

# Отключить автоматическое обновление runner'а
DISABLE_AUTO_UPDATE=false

# Заменить существующий runner с таким же именем
REPLACE_EXISTING=false
